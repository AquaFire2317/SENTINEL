# Dependencies installed by the Vercel build (see vercel.json).
# The serverless function serves the API; the model provider is selected with
# SENTINEL_MODEL_PROVIDER. `local` (deterministic) needs no extra SDK.
flask>=3.0,<4
strands-agents>=1.0,<2
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
boto3>=1.34,<2
# OpenAI-compatible providers (OpenAI, OpenRouter, custom endpoints):
openai>=1.40
